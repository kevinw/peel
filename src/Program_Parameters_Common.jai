MEMORY_DEBUGGER :: false;
TEMP_ALLOCATOR_POISON_FREED_MEMORY :: false;

#if OS == .WINDOWS && !RELEASE {
    #import "d3d12"()(INCLUDE_DEBUG_BINDINGS = true);
}

#import "Basic"()(
    MEMORY_DEBUGGER = MEMORY_DEBUGGER,
    TEMP_ALLOCATOR_POISON_FREED_MEMORY = TEMP_ALLOCATOR_POISON_FREED_MEMORY);
